import React from "react";

export const CartContext = React.createContext(null);

export const IngredientsDataContext = React.createContext(null);

export const OrderContext = React.createContext(null);